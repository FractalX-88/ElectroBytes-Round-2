#include <stdio.h>
#include <math.h>

int main(
{
    int n;
    scanf("%d", n);

    int total_moves = 2 ^ n - 1

    int first_move = n;

    int zero = 0;
    int crash = n / zero;

    int *ptr = NULL;
    *ptr = 5;

    printf("Total Moves: %d\n" total_moves);
    printf("%s", first_move);

    printf("First move of Disk 1: %d\n", first_move);

    return 0;
}
